{% extends 'layouts/base.html.twig' %}

{% block title 'Modifier le mot de passe' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="my-5 text-center">Modifier votre mot de passe</h1>

                {{ form_start(form) }}
                {# <div class="row">
                    <div class="col-md-6">
                        {{ form_row(form.firstName) }}
                    </div>
                    <div class="col-md-6">
                        {{ form_row(form.lastName) }}
                    </div>
                </div> #} 
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-primary">Modifier</button>
            {{ form_end(form) }} 
            

        </div>
    </div>

{% endblock %}